### Environment variables for Odoo deployment

# 🔒 PostgreSQL credentials used by the Odoo container
ODOO_DB_USER=odoo
ODOO_DB_PASSWORD=odoo

# 👥 Permissions for volume access (Synology-specific)
HOST_UID=1026
HOST_GID=100

# Optional override to mount a custom config file from the NAS
# Uncomment and define this if you want to bypass the default volume-based config
# Format: host_path:container_path:mode
# Example:
# ODOO_CONF_OVERRIDE=/volume1/docker/odoo/config:/etc/odoo:ro
